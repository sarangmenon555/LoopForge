FROM python:3.11-slim

WORKDIR /app

# Install dependencies
RUN pip install --no-cache-dir starlette uvicorn httpx

# Copy proxy script
COPY proxy.py .

# Expose port
EXPOSE 9009

# Run proxy
CMD ["python", "proxy.py"]
